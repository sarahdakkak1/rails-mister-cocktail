  <h1>Cocktails <span>Mania</span></h1>

</div>

<div class="row">
  <% @cocktails.each do |cocktail|%>
        <div class="col-12 col-md-6 col-lg-3">

    <div class="card-category" style="background-image: url('<%= cl_image_path cocktail.photo.key, height: 300, width: 400, crop: :fill %>')">
      <%= link_to cocktail.name.capitalize, cocktail_path(cocktail), class: "text-white text-decoration-none" %>
</div>
</div>
  <% end %>

